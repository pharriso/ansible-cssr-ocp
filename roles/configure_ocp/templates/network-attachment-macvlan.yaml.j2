apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: {{ item }}-macvlan
  namespace: {{ cssr_namespace }}
spec:
  config: |-
    {
    "cniVersion": "0.3.1",
    "name": "macvlan-net",
    "type": "macvlan",
    "master": "{{ item }}",
    "mode": "bridge"
    }
