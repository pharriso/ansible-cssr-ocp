#jinja2: trim_blocks:False
{
      "name": "{{ cssr_routername }}",
      "interNodeSecurity": "external",
      "serviceRoute": [
          {% for dept in cssr_depts %}
          {
              "name": "SR-{{ cssr_client }}-{{ site }}-lan{{ cssr_site_id }}{{ dept.dept_id }}-server1",
              "serviceName": "{{ cssr_client }}-{{ site }}-lan{{ cssr_site_id }}{{ dept.dept_id }}-server1",
              "nextHop": [
                  {
                      "nodeName": "{{ cssr_pod_name }}",
                      "interface": "lan{{ cssr_site_id }}{{ dept.dept_id }}-int"
                  }
              ]
          }{% if not loop.last %},{% endif %}
           {% endfor %}
      ]
}
